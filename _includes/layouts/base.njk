<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <script src="/js/theme.js" defer></script>
  <script src="/js/lang.js" defer></script>
</head>
<body>
  <header class="site-header">
    <div class="header-content">
      <div class="branding">
        <img src="/css/img/logo.png" alt="DAKODA Logo" class="logo" />
        <h1 class="site-title"><span>DAKODA</span> Infrastructure</h1>
      </div>

      <div class="header-actions">
        <div class="search-container">
          <input type="text" placeholder="Search..." class="search-input" />
          <span class="search-icon">🔍</span>
        </div>
        <div class="lang-toggle">
          <button class="lang-btn" onclick="switchLang('de')">DE</button>
          <button class="lang-btn" onclick="switchLang('en')">EN</button>
        </div>
      </div>
    </div>
  </header>

  <div class="container">
    <aside class="sidebar" id="sidebar">
  <div class="sidebar-header">
    <h3>Navigation</h3>
    <button id="toggleSidebar" title="Collapse Sidebar">⇤</button>
  </div>
  <ul>
    <li><a href="/index.de.html">Home</a></li>
    <li><a href="/repository.de.html">Repository</a></li>
    <li><a href="/dashboard.de.html">Dashboard</a></li>
    <li><a href="/notebooks.de.html">Notebooks</a></li>
    <li><a href="/docs.de.html">About Us</a></li>
  </ul>
</aside>

    <section class="main-content">
      {{ content | safe }}
    </section>
  </div>

  <footer>
    <div class="sponsor-footer">
      <span>Gefördert von:</span>
      <img src="/css/img/sponsor1.png" alt="Sponsor 1" class="sponsor-logo" />
      <img src="/css/img/sponsor2.png" alt="Sponsor 2" class="sponsor-logo" />
    </div>
    <p>© DAKODA Infrastructure 2025</p>
  </footer>

  <script src="/js/animations.js"></script>
  <button onclick="scrollToTop()" id="scrollTopBtn" title="Go to top">⬆️</button>

  <script>
    const scrollBtn = document.getElementById("scrollTopBtn");
    window.onscroll = function () {
      scrollBtn.style.display = (document.body.scrollTop > 100 || document.documentElement.scrollTop > 100) ? "block" : "none";
    };

    function scrollToTop() {
      document.body.scrollTop = 0;
      document.documentElement.scrollTop = 0;
    }

    function switchLang(targetLang) {
      let path = window.location.pathname;

      // Root fallback
      if (path === "/" || path === "/index.html") {
        window.location.href = `/index.${targetLang}.html`;
        return;
      }

      // Replace the language suffix
      const newPath = path.replace(/(de|en)\.html$/, `${targetLang}.html`);

      if (newPath === path) {
        // fallback if no lang-specific file found
        window.location.href = `/index.${targetLang}.html`;
      } else {
        window.location.href = newPath;
      }
    }
  </script>

<script>
  const sidebar = document.getElementById("sidebar");
  const toggleBtn = document.getElementById("toggleSidebar");

  toggleBtn.addEventListener("click", () => {
    sidebar.classList.toggle("collapsed");
    toggleBtn.textContent = sidebar.classList.contains("collapsed") ? "⯈" : "⇤";
  });
</script>

</body>
</html>
